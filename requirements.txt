fastapi>=0.110
uvicorn[standard]>=0.27
gunicorn>=21.2
slowapi>=0.1.9
SQLAlchemy>=2.0
psycopg[binary]>=3.1
yfinance>=0.2.54
# yfinance 0.2.54+는 curl_cffi가 있으면 크롬 TLS 지문으로 요청한다.
# 없으면 requests로 폴백하는데 야후가 그 지문을 우선적으로 차단한다.
curl_cffi>=0.7
pandas>=2.0
# 하원 PTR PDF의 텍스트 추출(전자 제출분). 스캔 제출분은 추출 불가로 표시한다.
pypdf>=4.0
# 뉴욕 연은 침체 확률 파일(allmonth.xls)은 구형 BIFF 워크북. xlrd 2.x는 .xls 전용이다.
xlrd>=2.0
# zoneinfo("America/New_York")가 OS tz 데이터 없는 환경(Windows 등)에서도 돌게 한다.
tzdata

numpy>=1.24
statsmodels>=0.14
