# Mulmit 로드맵 — 위시리스트 판정표

작성일: 2026-08-18. 운영자 위시리스트 전체를 등록부 원칙(도달 가능 ≠ 재배포 가능,
fail-closed, 수치 발명 금지)으로 판정한 문서다. 실행 순서의 단일 진실.
관련: `docs/DATA_SOURCE_REGISTER.md`, `docs/PLAN_KR_SECTIONS.md`(hlkr 매핑),
`docs/PLAN_SITE_SPLIT.md`(리디자인).

## 확정 실행 순서 (2026-08-18 합의)

1. **ETF 시세** — 운영자 활용신청(아래 §1) 후 기존 FSC 패턴 재사용. 공수 小.
2. **페이지 분리 + 리디자인** — `/`(랜딩) · `/kr` · `/us`. "가장 급함"으로 지정.
   설계는 `docs/PLAN_SITE_SPLIT.md`.
3. **US PTR (미국 정치인 거래)** — 상·하원 STOCK Act 공시 파서. 미국 페이지의
   킬러 콘텐츠.

## 판정표

| # | 항목 | 판정 | 근거·경로 |
|---|---|---|---|
| 1 | ETF 시세 | ✅ **완료 2026-08-18** | `/api/kr/etf` 보드. [증권상품시세정보](https://www.data.go.kr/data/15094806/openapi.do) 활용신청 승인, 기존 FSC lane·키. 채권·일반상품도 승인(미사용 — 국고채·금현물 후보) |
| 2 | 페이지 분리+리디자인 | ✅ **P1~P4 완료 2026-08-19** · 통합 검색 2026-08-22 | `/`·`/kr`·`/us` 분리(P1), 세션 배지·H.10 티커·컴팩트 히어로·존 카드(P2), canonical/OG·robots·sitemap(P3), 모바일 하드닝(P4) — 단계표는 `PLAN_SITE_SPLIT.md`. 이후 /crypto 추가(#16), 브랜드 락업(#109) |
| 3 | 미국 정치인 보유·거래 | ✅ **하원 완료 2026-08-18** | `/api/us/ptr` — 하원 인덱스+전자 PDF 거래 파싱(엄격 매칭, EIGA §105(c) 고지 동봉). **상원 eFD는 봇 차단(403)으로 보류 — 우회 안 함**(등록부 §3.14). 한국은 구조화 데이터 부재로 보류 확정 |
| 4 | SEC EDGAR 재무제표 | ✅ **완료 2026-08-19** | `/api/us/fundamentals/{ticker}` + /analytics 패널 — companyconcept XBRL, 내부자 lane과 같은 큐·게이트. **8-K 피드도 완료**(`/api/us/events`, 내부자 수집 응답 재사용·추가 요청 0). AI 요약은 별도 후속 |
| 5 | fundamental 분석 (재무비율) | ✅ **양국 완료 2026-08-19** | 미국 = #4(마진·EPS), 한국 = `/api/kr/fundamentals/{code}`(DART 주요계정, 연간·연결 우선). 가격 의존 비율은 권리상 불가, 목표가·컨센서스는 증권사 리포트 저작권이라 ❌ |
| 6 | 경제 캘린더 | ✅ **완료 2026-08-19** | `/api/calendar` + /us 섹션 — CPI·고용·GDP·PCE는 FRED 릴리스 일정 자동, FOMC·금통위는 공식 페이지 검증 큐레이션(확인일 동봉). 신호 피드(#12)의 뼈대 |
| 7 | 실시간 접속자 수 | ✅ **완료 2026-08-19** | `/api/presence` — 익명 무작위 id 30초 하트비트, 90초 창의 열린 브라우저 수(사람 수 아님 명시). 전 페이지 헤더 배지 |
| 8 | 바이오 (ClinicalTrials.gov·openFDA·PubMed·Federal Register·식약처) | ✅ 라인 5종 라이브 · **2026-08-23 탭에서 강등**(아래) | ✅ **Phase 1·2 + 식약처 전부 라이브 2026-08-22** (임상 34 스폰서·156건, FDA 60일 NDA 16·BLA 7·ANDA 117, PubMed 150건·적중 34, 자문위 공고 11건, 식약처 30일 112품목·허가 72·신약 1·희귀 2) | 계획 `PLAN_BIO_SECTION.md`. **Phase 1**: `/bio` 페이지 + `/api/bio/trials`(ClinicalTrials.gov v2, 워치리스트 34 스폰서의 최근 14일 갱신 중재 2·3상(스폰서당 최대 8건, 한국/글로벌 필터) — 약관 4조건[출처·최신성·처리일·수정 명시] 동봉, 등록부 §3.22 `DS-2026-014`) + `/api/bio/fda`(openFDA drugsfda 최근 60일 NDA·BLA 원 신청 승인, CC0, §3.23 `DS-2026-015`). 게이트 `BIO_SECTION_ENABLED`·`CLINICALTRIALS_ENABLED`·`OPENFDA_ENABLED` 기본 OFF → 운영자가 켬. 한국어 표시명·상장 라벨은 Mulmit 참고 라벨. **Phase 2 (라이브 2026-08-22 14:27 KST — 운영자 게이트 ON)**: `/api/bio/trials`에 임상별 **PubMed 서지**(NCT[si] 검색, 제목·저널·일자·PMID만·초록 비표시, ET 야간 창·0.4초 간격, §3.25 `DS-2026-017`) + `/api/bio/adcomm` **FDA 자문위원회 회의 공고**(Federal Register API, 공개 도메인·키 없음, §3.24 `DS-2026-016` — fda.gov 달력은 봇 감지로 보류). 게이트 `PUBMED_ENABLED`·`FEDERAL_REGISTER_ENABLED`. **식약처 품목허가** `/api/bio/mfds`(라이브 2026-08-22 15:3x KST — 운영자 게이트 ON, 첫 패스 30일 112품목): 운영자 활용신청 승인 후 서버 실측 — 상세 엔드포인트 `item_permit_date`로 최근 30일 하루씩(≈30회/일), 허가/신고·전문/일반·신약·희귀·주성분, '주목' 기본 보기, 등록부 §3.26 `DS-2026-018`(키는 FSC 키 재사용). 보류: PDUFA 달력(공개 원천 없음), 스폰서↔티커 자동 매핑, 초록 전문 |
| 9 | 코스피 야간선물 (프리장) | ⚠️ 대체 완료 · **NXT 조사 종결 2026-08-22** | CME 야간선물 시세 = 거래소 라이선스(불가). **KR200 퍼프가 이미 합법 대체물로 라이브**. NXT(넥스트레이드) 시세 조사 결과(등록부 §6.3): 정보포털 **계약 상품**만 존재 — 웹사이트용(CASE 3, 비로그인 공개) **고정비** 계약, 무상 프로모션 2025-03~2027-02이지만 최초 계약에 2027-03~2028-02 유상 1년 포함·금액 비공개 → `license_required`, 계약 없이는 프리장/애프터마켓 현물 시세 표시 불가. 운영자 선택: Contact Sales로 고정비 문의(예산 내면 진행). 증권사 화면·크롤링 경로는 금지 원칙 그대로 |
| 10 | 뉴스 (종목 태깅) | ✅ **양축 완료 2026-08-20** — 영문=GDELT 15분 벌크, 한국어=정부 보도자료 RSS(`/api/kr/press`). 후보 전수 판정 종결: NCP HUB ❌(광고 금지 특약)·텔레그램 스쿽 ❌·KRX 플로우 ❌(전부 등록부 §6). 유료 잔여 후보는 DeepSearch뿐 | 운영자 스펙: 실시간 뉴스 피드 + **연관 종목 등락률 태그 칩** 노출. 구현 원칙 — 표시는 제목+출처+원문 링크까지(본문·요약 재게시는 언론사 저작권), **등락률 칩은 뉴스 소스가 아니라 우리 데이터**(장중=퍼프 참고가, 장후=공식 종가·15:30 참고선)로 계산해 권리 무관. 종목 매칭은 헤드라인의 회사명↔로스터 사전(FSC kr_listings·EDGAR 티커맵) 닫힌 매칭. 소스 후보(Marketaux·Finnhub·Financial Juice·네이버 검색 API)는 등록부 §6.1 약관 검토가 선행 게이트 |
| 11 | roic.ai / 어닝콜 원문 | ❌ **현시점 기각 2026-08-22** | 조사 결과(등록부 §6.4): ToS(2026-04-07) §7·§8 재배포·재판매 금지(명시 승인 필요), 상업 이용 API는 **Enterprise 개별 견적**만(Free/$29/$89는 비상업) → 예산 밖. 어닝콜 트랜스크립트는 벤더·발행사 저작물이라 전문 표시 부적합. 재무비율은 #4 XBRL 자체 산출로 대체 완료. 재검토: self-serve 상업 티어 신설·예산 상향 시 |
| 12 | 실시간 신호 피드 | ✅ **통합 피드 완료 2026-08-19** | `/api/feed` — 8-K·주요사항보고·PTR·연금 4소스 시간순 병합 + 캘린더 upcoming 분리, 홈 '지금 일어나는 일' 섹션, 항목→종목 허브 링크, fail-soft(빈 소스는 비표시). **후속 확장 완료(2026-08-20)**: 지수 급변(3% 계단+회복 히스테리시스 0.5)·뉴스·보도자료·대량보유 5%(신규 진입·목적 변경·±2%p 큐레이션) 합류, 전체/한국/미국·해외 지역 필터, 뉴스류 15분 수집 주기 |
| 13 | "전부 실시간" | ⚠️ 경계 명시 | 프론트 실시간화(SSE/짧은 캐시)는 가능. **소스 실시간화는 권리 문제**: HIP-3는 이미 준실시간, 한국 현물·지수 실시간은 KRX/NXT 계약 전 불가, 공식 환율은 정의상 고시값 |
| 14 | 예측 이벤트 (상금 100만원) | ⏸ 장기 | 경품·사행성 규제 법률 검토 + 계정 시스템 필요. 데이터 문제가 아니라 컴플라이언스 문제 |
| 15 | Polymarket 베팅 확률 표시 | ❌ **불가 (2026-08-18 판정)** | 방송통신심의위원회가 **같은 날 접속 차단을 의결** — 사행성 인정, 형법상 도박개장·국민체육진흥법 유사행위 해당 판단([ZDNet](https://zdnet.co.kr/view/?no=20260818173958), [이데일리](https://www.edaily.co.kr/News/Read?newsId=04637926645548632)). 프랑스·호주·독일도 차단. 차단 의결된 도박 사이트의 배당 데이터를 국내 대상 사이트에 표시하는 것(우리 원칙상 출처 링크 필수)은 불법정보 유통·도박 홍보로 해석될 위험이 크고, 광고 심사(AdSense 도박 정책)에도 치명적. **CFTC 규제 거래소(Kalshi류)의 예측시장 데이터는 성격이 다르나**, 국내법 관점의 사행성 조장 문제와 데이터 재배포 약관을 이중으로 검토하기 전에는 같은 결론으로 보류 |
| 16 | 크립토 섹션 (BTC·ETH 시세, 공포·탐욕, 펀딩비, 도미넌스, 가스, 청산, DVOL) | ✅ **Phase 1·2·2b·3a·3b 라이브 2026-08-22** (청산·DVOL은 회신 대기) | **Phase 2**: `/api/crypto/structure`(CoinMarketCap 도미넌스·총시총 — 운영자 키 발급 후 `CMC_ENABLED`, 등록부 §3.20) · `/api/crypto/kimchi`(업비트 원화 시세·김치프리미엄 USDT 기준/공식환율 기준 — **운영자 위험수용으로 개방 2026-08-22**, `DS-2026-012`, 등록부 §3.19; 두나무 문의는 기록용) · 가스 스트립은 퍼블릭 RPC 대신 **운영자 RPC 계정 URL 주입형 lane**(`/api/crypto/gas`, `CHAIN_GAS_ENABLED`, §3.21 — **Alchemy 계정으로 2026-08-22 ON**, `DS-2026-013`, 3개 체인 라이브 — Base/Arbitrum은 2026-08-22 앱 네트워크 활성화로 복구) · 청산 집계·DVOL은 문의 초안 발송 대기(Gmail 임시보관함). **Phase 1 구현**: `/crypto` 페이지 + `/api/crypto/{overview,sentiment,volatility}` — HL 네이티브 퍼프 10종 카드·펀딩/OI/예상펀딩 표·ETH/BTC, alternative.me 공포·탐욕(등록부 §3.18 `DS-2026-010`), 실현 변동성(√365)·BTC 대 합성자산 상관(저장 일봉 파생). 게이트 `CRYPTO_SECTION_ENABLED`·`ALTERNATIVE_ME_ENABLED`(web·ingest 공통, 서버 .env에서 켬). 설계·추천안 검증·실측은 `docs/PLAN_CRYPTO_SECTION.md`, 문의 초안 `docs/INQUIRY_CRYPTO_SOURCES.md`. 판정 요지: "크립토는 규제가 적다"는 반만 맞음 — 라이선스 요금은 0이지만 거래소 약관이 공개 재표시를 **명시 금지**(Binance 광고·레퍼럴 수익 사이트 이용 금지, OKX §9.4, Coinbase Market Data Terms, Deribit §4.6 개인 용도 한정, Bybit 상업 이용 금지)하고 CoinGecko·CoinPaprika·DefiLlama·Etherscan 무료 티어는 비상업. **Phase 1(신규 권리 없음)**: HL 네이티브 퍼프 가격·24h·펀딩·OI·교차거래소 예상펀딩(`predictedFundings`, interval 동봉·APR 환산) + alternative.me 크립토 공포·탐욕(상업 허용·출처 값 옆 고정) + ETH/BTC·실현변동성·교차시장 상관(HL 캔들 자체 계산). **Phase 2(운영자 액션 후)**: 도미넌스=CMC Basic(무료·상업 허용 명시, 키 발급), 김치프리미엄=Upbit 시세(문의/위험수용 — USDT 분모로 실시간 환율 소거), 가스=퍼블릭 RPC·mempool.space, 청산 집계=Coinalyze(문의), DVOL=Deribit 서면 승인. **불가**: Binance 청산 스트림 브라우저 직결(약관 + 추천 URL `fstream…/ws/!forceOrder@arr`은 2026-04-23 라우팅 전환으로 사망, 실측 0건/285s — 새 `/market` 경로만 58건/60s + 심볼당 1초 1건 스냅샷이라 합계 표시 불가), CNN F&G 스크래핑·Cboe VIX 교차(기존 결정 유지, 대체쌍 = #17 게이지·OFR·실현변동성). 아키텍처: 브라우저 직결은 HL WS만, 나머지 서버 relay(미신고 해외거래소 사이트 차단 심의 2026-07·레퍼럴 형사처벌 동향, Upbit Origin 요청 10초 1회 제한). 재검토 2026-09-16(회신)·2026-09-30(차단 심의) |
| 17 | 변동성·심리 대체 지표 (VIX·공포탐욕 불가 대응) | ✅ **완료 2026-08-21** (운영자 실사용 피드백에서 파생) | Cboe VIX·ICE 하이일드는 영구 `license_required`라 공개 페이지에서 **숨김**(#100). 대체 3종 — ① **OFR 금융스트레스지수 lane**(미 재무부 연방 저작물, 등록부 §3.17 `DS-2026-009`): 변동성·신용·종합 카드(#101) ② **실현 변동성 카드**: HIP-3 일봉 20개 로그수익률 σ×√252, "VIX 아님" 표기(§5.4.1, #102) ③ **Mulmit 시장 심리 게이지(실험)**: OFR 변동성·신용 + 퍼프 모멘텀·실현변동성·주식 대 금, 자기 이력 백분위·동일 가중, CNN 명칭·밴드 불복제(§5.4.2, `/api/market/sentiment`, #103). 전제가 된 **HIP-3 일봉 이력 lane**은 DS-2026-001 개정(무응답≠OFF, 명시적 거절만 OFF)으로 개방(#98). 0 중심 지수 변화 표기를 %→포인트로 교정 |

## §1 ETF 활용신청 (운영자 액션, 1회)

1. https://www.data.go.kr/data/15094806/openapi.do 접속 (금융위원회_증권상품시세정보)
2. 로그인 → **활용신청** → 활용목적은 기존 주식시세정보 신청 때와 동일하게
   (mulmit.com 시장 대시보드, 출처 표기) → 자동승인
3. 키는 기존 data.go.kr 키와 동일하게 발급되므로 서버 변경 불필요할 가능성 큼
   (같은 키로 호출만 열림 — 승인 후 확인)

## §2 React 전환 판정 — **지금은 하지 않는다** (2026-08-22 재확인)

운영자가 "SEO를 조금 포기하더라도" 다시 물었고, 다시 아니라고 답한다. 다만 근거의
무게가 바뀌었다 — SEO는 두 번째 이유고, 첫 번째는 **불만의 원인이 렌더링 모델이
아니라는 것**이다.

- **호소된 문제는 전부 정보구조·디자인 시스템 문제였다.** 실측 원인: 웹폰트 미로딩
  (`--sans`에 Pretendard가 있으나 `@font-face` 없음 → 윈도우는 맑은 고딕), 본문 14px에
  라벨·고지 8~10px, `Intl` compact이 지수를 "7.66천"·"2.92만"으로 표기, `/us`의 카드
  22장이 전부 같은 무게. React는 이 중 하나도 고치지 못한다.
- **프론트 테스트가 0개다.** 628개 테스트는 전부 백엔드다. 안전망 없는 3,575줄
  단일 파일(렌더러 40여 개)을 통째로 재작성하는 것이 지금 할 수 있는 가장 위험한
  선택이다.
- **SEO는 여전히 유입 전략이다**("주말 삼성전자 얼마"). 서버 렌더 `/stock/{코드}`
  허브와 사이트맵을 버려서 얻는 것이 없다.
- **대신 한 것**: 바닐라를 유지하되 디자인 토큰(`tokens.css`)·콘솔 셸
  (`console.css`·`console.js`)을 분리하고, monitor.js는 엔진으로 남긴 채
  `mulmit:render` 이벤트로만 연결했다. 상태를 복제하지 않아 두 파일이 어긋나지 않는다.
- **재검토 조건**: 관심종목·알림·계정처럼 진짜 상태가 있는 기능이 들어올 때. 그때도
  **CSR React가 아니라 Astro(아일랜드)** 다 — 정적 HTML 기본 + 인터랙티브 위젯만
  하이드레이션이라 SEO를 포기할 이유가 애초에 없다.

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-23 | **V-KOSPI 200 조사** — 금융위 지수시세정보 lane이 이미 열려 있어 데이터셋에 있기만 하면 새 권리 없이 붙일 수 있었으나, 저장된 지수 168개 전수 확인 결과 **없음**(분류 4종에 변동성 계열 없고 '변동성' 포함 이름 0건). KRX 산출 지수라 공공데이터 개방 대상이 아니며 코스콤/KRX 견적 문의에 종가와 같은 범위로 얹음. 대안인 `kr200_realized_vol`은 **실현** 변동성이라 내재 변동성의 대체재가 아님(§5.4.1 수준 비교 금지) | 등록부 §6.5 |
| 2026-08-23 | **표 폭과 경제 캘린더** — 표가 `width:100%`라 남는 폭이 모든 열에 고루 퍼지고 있었다. 코스피 지수군은 내용이 566px인데 1286px로 늘어나, 값 하나가 제 머리글에서 200px 떨어져 앉았다(실측). 남는 폭을 한 열에 몰아주는 방법도 대 봤지만 더 나빴다 — 긴 글 열이 부풀면 그 뒤의 원문 링크·지역·유형이 본문에서 700px 밀려난다. **남는 폭은 표 밖에 둔다**(내용 폭 + `max-width:100%`). 예외는 보드 카드 하나 — 카드 폭을 격자가 정하므로 그 안에서는 채운다. 지수군은 두 표를 **나란히**(auto-fit 520px) 세워 세로 길이를 반으로 줄였다. **경제 캘린더는 달력이 됐다** — 표는 "다음이 며칠인가"만 알려 주고 "그때까지 얼마나 비었나"를 못 보여 준다. 월 격자 + 달 넘기기, 지표 발표/정책회의 색 구분, D-day, 좁은 화면은 점 + 아래 목록, 전체 20건은 서랍에 표로(크롤러용). **함께 고침**: D-day가 `toISOString()`에 `getTimezoneOffset()`까지 더해 보는 사람 시간대만큼 한 번 더 밀리던 것(한국 오전 9시 전이면 하루 어긋남) |
| 2026-08-23 | **한 칸에 한 가지만 — 나머지 네 곳** — 같은 패턴을 전수로 찾아 마저 나눴다. 8-K(`<code>BA</code> BOEING CO` → 티커 열), ETF(이름+코드 → 종목코드 열), 하원 PTR 의원 칸(이름+지역구 → 지역구 열), PTR 종목 칸(종목명+티커+보유자 → 티커·보유자 열). 특히 **보유자**(본인/배우자/공동)는 같은 거래를 다르게 읽게 만드는 값이라 작은 칩으로 붙어 있을 자리가 아니었다 — 이제 한 눈에 배우자·공동 거래가 보인다. 자산명에 `AT&T Inc. (T)`처럼 티커가 이미 들어 있어 티커 열과 겹치지만, 그 문자열은 PTR 원문 그대로라 손대지 않는다 |
| 2026-08-23 | **한 칸에 한 가지만 — 회사 열에 두세 가지가 뭉쳐 있던 표 셋** — 주요사항보고는 `145170 노브랜드`처럼 종목코드와 회사명이 서로 다른 글꼴로 한 칸에 붙어 있었고, 국민연금·대량보유 5%는 `셀트리온 **원문** 유가증권`으로 회사명·원문 링크·시장 셋이 한 칸에 있었다. 굵기와 글꼴이 달라 읽는 사람이 매번 경계를 다시 찾아야 했다. 열로 나눴다 — 주요사항보고에 `종목코드`, 연기금에 `시장`, 셋 모두에 `원문` 열. 쓰이지 않게 된 `.krp-source`도 걷어냈다 |
| 2026-08-23 | **`/bio` 재설계 — 기록 보관소에서 '무엇이 언제 결정되나'로** — 진단: 네 섹션이 전부 *이미 일어난 일*의 목록이었고(임상 갱신·FDA 승인·식약처 허가), 앞을 보는 값은 자문위 예정 하나뿐이었다. 그런데 바이오 종목을 움직이는 것은 아직 나지 않은 결정이다. 게다가 순서가 임상(혼재)→자문위(미)→FDA(미)→식약처(한)라 한/미가 번갈아 나왔다. **① 다가오는 결정**(신규): 흩어져 있던 앞을 보는 값 둘을 한 시간축에 놓는다 — FDA 자문위 확정일 + 임상 1차 완료 예정일(180일 창, 실측 자문위 2건·90일 내 1차 완료 5건). 둘은 확실성이 다르므로 배지로 구분하고 "계획값이며 1차 완료 ≠ 결과 발표"를 근거 줄에 박았다. 등록부가 월까지만 준 값(`2026-10`)은 '중'을 붙여 없는 정밀도를 만들지 않는다. 날짜 없는 공고는 오지 않는다. **② 시장별 구역**: 한국(식약처+국내 스폰서 임상 6건) / 미국·글로벌(FDA+글로벌 임상 144건+자문위). 지역 필터 버튼을 없애고 **섹션**으로 나눴다 — 버튼이 있어도 "지금 무엇을 보는지" 기억하는 부담은 남는다. 요약 숫자도 지역별로 다시 세운다(한국 섹션이 여섯 줄 보여 주며 '스폰서 34'라고 적으면 안 된다). 순서·구역 회귀 테스트 3개 |
| 2026-08-23 | **패널 안쪽 여백 전수 정리 + `/bio`가 리디자인 밖에 있던 것** — 한 패널만 고치면 또 나오므로 **브라우저에서 모든 패널의 '테두리→글자' 최소 거리를 재는 감사**를 돌렸다(숨은 caption·absolute 요소 제외). 기준 17px 미만이 나온 곳: 게이지 패널 3곳(스트레스·심리·공포탐욕, 1px)·암호화폐 뉴스(3px)·변동성 푸터(1px)·캘린더 격자(7px, 위의 달 이름은 17px이라 어긋남)·홈 피드 필터(14px)·바이오 출처 4곳(1px). **함정 둘**: ① `.panel > .panel-footer`가 직계 자식만 잡아 한 겹 더 들어간 푸터를 놓쳤다(자손 선택으로) ② **`/bio`는 `monitor.css`만 부르고 있었다** — 어제 만든 테스트가 '인라인 `<style>`이 있는 페이지'만 봐서 인라인 스타일이 없는 bio가 또 새어 나갔다. 규칙을 **"monitor.css를 부르는 페이지는 토큰·콘솔 시트도 그 뒤에"**로 일반화했다(법적 고지 3종은 legal.css로 별도 디자인이라 해당 없음). `monitor.html`도 함께 |
| 2026-08-23 | **`immutable`을 걷어냈다 — 배포 경합으로 옛 파일이 새 키에 1년 박힌다** — #160에서 `?v=` 붙은 정적 파일에 `max-age=31536000, immutable`을 걸었다. 그런데 `?v=`는 쿼리일 뿐이고 서버는 늘 같은 파일 하나를 준다 — **URL과 내용이 1:1이 아니다.** 배포 도중 HTML은 새 버전인데 파일이 아직 옛것인 찰나에 요청이 들어오면 브라우저가 옛 내용을 새 키로 받고, `immutable`이라 재검증도 없이 1년 고정된다. 실측: #170 배포 직후 실행 중인 `renderCryptoOverview`에 새 코드가 없었다(같은 URL을 `cache:"reload"`로 받으면 있었고, 캐시를 비우니 즉시 정상). 사용자는 캐시를 비우지 않는다. **`?v=` 자산은 `max-age=86400`**(하루면 ETag 재검증으로 자가 치유), 폰트만 `immutable` 유지 — 서브셋 92개는 이름이 곧 내용이라 경합이 성립하지 않는다 |
| 2026-08-23 | **암호화폐 카드에 30일 추이선** — 카드가 값 하나와 배지뿐이라 밋밋했다. 원천 판단이 먼저였다: 자체 일별 누적(`crypto_regime`의 daily)은 2026-08-22에 시작해 **두 점**뿐이라 30일 선이 될 수 없다. Hyperliquid `1d` 봉은 **365일** 창을 주고 코인 상세 화면이 이미 같은 원천을 쓴다 — 그쪽으로 갔다. `history_symbols()`를 카드 코인 전부로 넓혀 기존 `hip3_history` lane이 함께 모으게 하고(새 lane 없음, 새 심볼 backfill도 이미 있음), `HISTORY_COINS`(변동성·상관 표본)는 손으로 고른 셋 그대로 뒀다 — 둘은 다른 질문에 답한다. 값은 **저장된 블롭에서만** 읽는다: 요청 경로에서 상류를 부르면 카드 열 개에 호출 열 번이 붙고, 하루 한 번 바뀌는 선 때문에 15초 캐시가 계속 깨진다. 선은 홈 타일과 **같은 함수**(console.js)를 부르고 기간 라벨까지 그쪽이 내준다 — 창을 자르는 규칙과 '104일→3개월' 규칙이 갈리면 같은 그림이 다른 뜻이 된다. 아직 안 모인 코인은 선 없이 카드만 선다 |
| 2026-08-23 | **상세 페이지 3,130개가 리디자인을 통째로 놓치고 있었다 + 국내 종목이 미국 조회로 가던 링크** — `/stock/{코드}`(2,953)와 `/crypto/{심볼}`(177)은 `monitor.css`만 부르고 tokens/console을 부르지 않았다. URL 수로는 사이트의 대부분이고 검색 유입이 닿는 면 전체다. 인라인 `<style>` **뒤에** 시트를 걸고(앞에 두면 같은 특이도에서 진다 — /analytics 함정 재현) 인라인의 `var(--mono)` 22곳을 `--num`으로, `font:` 단축 17곳에 자리폭 숫자를 함께 적었다. 옛 마스트헤드 부제도 제거. **딥링크 버그**: 종목 상세의 '위험 분석' 링크가 `/analytics?ticker=005380`으로 넘겼는데 저쪽에서 `ticker`는 **미국 상장사 전용** SEC EDGAR 입력란이다 — 국내 종목코드로 미국 내부자거래를 조회하고 있었다. `?kr=`로 바꾸고 분석 페이지가 그걸 읽어 국내 분석을 돌린다. **함께**: 상태줄(데이터 연결·마지막 갱신·새로고침)을 설명 문단이 아니라 **제목 줄**에 정렬(`align-items: flex-end` → `flex-start` + h1 줄 높이만큼의 min-height, 실측 오차 0px), 한국어 화면의 **크립토 → 암호화폐** 52곳(식별자·주석·로그·docs는 그대로). 타이포 테스트가 이 두 페이지를 안 보고 있어서 놓쳤던 것 — 목록에 넣었다 |
| 2026-08-23 | **지수군의 죽은 열을 살렸다 — `52주 범위`는 이름이 두 군데 틀렸다** — 스물한 행 모두 `—`였다. 파고 보니 ① **범위가 아니다**: 금융위가 연중 최저를 확정 전까지 `0` + 미래 날짜로 공표하고 `_positive_or_none`이 올바르게 걷어낸다(이미 `test_an_unfinalised_52_week_low_of_zero_is_null_not_a_record`에 기록돼 있었다). 최고는 21/21 실제 값이 온다. ② **52주가 아니다**: 원천 필드 `yrWRcrdHgst`는 "지수의 연중최고치"(공공데이터포털)이고 형제 필드가 전년말 대비(`lsYrEdVsFltRt`)인 것도 역년 기준이라는 뜻이다. 고가 날짜가 전부 2026년인 것만으로는 판정 불가 — 코스피가 올해 +62.6%라 강세장에서는 연중 최고와 52주 최고가 일치한다. 명세로 확인했다. 열을 **`연중 최고 대비`**(종가/연중최고−1)로 바꿨다. 최고값과 그 날짜는 표를 넓히지 않게 툴팁으로, 저가가 없는 이유는 푸터에 남긴다. 값은 정의상 0 이하라 등락색을 쓰지 않는다 — 빨강이면 "오늘 떨어졌다"로 읽힌다. 라벨이 되돌아가지 못하게 테스트 2개 |
| 2026-08-23 | **글꼴 전면 개편 — 부조화의 정체는 '등폭 스택 속 한글'이었다** — 수치·라벨 면이 시스템 등폭(윈도우 실측 Cascadia Mono)이었고, 그 줄에 한글이 섞여 있었다("저유동성 가능", "확정 종가 코스피 6,852.58", "마지막 갱신 …"). 등폭에 없는 한글은 대체 글꼴로 빠지므로 **한 줄 안에 두 글꼴**이 섰다. ₩도 등폭에서 같은 폭에 욱여넣느라 짓눌린다(40px 실측 Cascadia 23.4 vs Pretendard 36.5). `--num`(Pretendard) 도입, `var(--mono)` 98곳 → `var(--num)`, `--mono`는 식별자(티커)에만. 정렬은 잃지 않는다 — Pretendard tabular-nums는 열 자리 폭이 완전 균일(40px에서 전부 25.58px)이고, 계기판이 기대는 1em 띠 칸도 그대로다(실측 24.94px/38px). **함정**: `font:` 단축 속성이 `font-variant-numeric`을 normal로 되돌린다 — body에 켠 자리폭 숫자가 --num 자리마다 꺼져 있었다. 85곳에 함께 적었다. **함께**: 배지 재설계(8.5px 등폭 + 한글에 무의미한 uppercase → Pretendard 12px 알약, 심각도는 옅은 바탕으로 · 상시 고지 칩은 조용한 윤곽선 유지), 접속자 배지에 맥박 점(reduced-motion이면 정지), **/kr 카드의 보조 퍼센트 제거**(공식 종가가 늦으면 '15:30 이후' 하나만 — 전전날 마감 대비는 근거표에 값으로 남는다. ADR 프리미엄은 다른 것을 재므로 예외) |
| 2026-08-23 | **캐시 지시 명시 — 배포가 사용자에게 늦게 닿던 것** — HTML과 정적 파일에 `Cache-Control`이 없어 브라우저가 **휴리스틱 캐싱**(대략 `(지금 − Last-Modified) × 10%`)을 쓰고 있었다. 배포 직후엔 창이 짧지만 열흘 손대지 않은 파일은 하루치가 되고, 재방문자는 하루 지난 HTML + 그 HTML이 가리키는 옛 `?v=` 에셋을 통째로 받는다. 깨지지 않아서(둘이 같은 판이라 정합은 맞다) 더 안 잡힌다 — 실측: 배포 1시간 뒤 브라우저가 `v=20260823-22`를 그리는 동안 `fetch(cache:"reload")`는 새 HTML을 받았다. 미들웨어 하나로 **HTML은 `no-cache`**(쓰기 전에 물어봄), **`?v=` 붙은 정적과 폰트는 `immutable` 1년**, 나머지 정적은 1시간. 라우트가 직접 정한 값(`/glossary` 3600, `/stock` 600 등)은 건드리지 않는다. **함정**: `StaticFiles`는 `If-None-Match`를 스스로 보지만 라우트가 돌려주는 맨 `FileResponse`는 보지 않는다(starlette 0.46 실측) — 그대로 뒀으면 `no-cache`가 '물어보기'가 아니라 '매번 전체 다시 받기'였다. 미들웨어에서 약한 비교로 304를 만들어 준다(16KB → 0B 확인) |
| 2026-08-23 | **경제 캘린더 홈 배치 + 패널 안쪽 여백** — 캘린더를 홈에도 놓았다(피드 아래: 방금 일어난 일 → 다가올 일정). 홈 피드 안에 있던 D-day 칩 줄(`#feed-upcoming`)은 같은 데이터라 뺐다 — `/api/feed`의 `upcoming`은 캘린더 이벤트 그대로였다. 히트맵·캘린더가 홈과 `/us`에 겹치는 것은 **그대로 둔다**: 렌더러가 하나라 어긋날 수 없고, 홈은 훑는 자리·`/us`는 맥락 안에서 읽는 자리다. **패널 안쪽 여백** — `.panel`이 padding 0이라 표의 셀 padding 10px에만 기대고 있었고, 제목(`.kridx-group`)과 푸터는 그마저 없어 테두리에 닿아 있었다(실측: 패널 왼쪽 629px, "대표 지수" 630px). 표는 행 구분선을 끝까지 긋는 편이 나으니 full-bleed로 두고 바깥쪽 셀 padding으로 같은 들여쓰기를 만들었다 — 제목·열 머리글·값·푸터가 모두 테두리+17px에 선다. 위험 고지 배지 줄은 아래 여백이 0이라 카드 격자가 배지에 붙어 있었다(배지 bottom = 카드 top 394px) |
| 2026-08-23 | **문구 사전 회귀 테스트 + 안 풀리던 `data-i18n` 11개** — `t()`는 영어 키가 없으면 조용히 한국어로 되돌아간다. 두 사전(monitor.js `TEXT`, index.html `I18N`)의 키·자리표시자 대조와 "화면이 부르는 열쇠가 사전에 있는가"를 테스트로 박았다. 그 테스트가 바로 잡아낸 것: **`/glossary`는 monitor.js를 부르지 않는데 탭·푸터 11곳에 `data-i18n`이 달려 있어** EN으로 바꿔도 그 자리만 한국어였다 (본문이 이미 쓰는 `.lang-ko`/`.lang-en`으로 교체). `stock.html`의 `nav.crypto` 한 개도 같은 이유로 무효라 제거 |
| 2026-08-23 | **#8 바이오 배치 판단 — 탭에서 강등, 페이지는 유지** — 유입 데이터로는 못 정한다(14일 총 32PV, `/bio` 0인데 `/kr`·`/us`도 각 1 — n=32에서 0과 1은 잡음). 구조 근거로 정했다: ① 값이 움직이지 않아 테이프·보드·타일·계기판 어휘가 하나도 맞지 않는다 ② 신호 피드에 한 줄도 합류하지 않았다(실측: `kr_material`·`news`·`kr_holdings`·`us_8k`·`kr_press`만) ③ 탭 6칸 중 하나를 쓰면서 나머지와 연결이 없다. **탭 줄에서만 뺐다** — 페이지·URL·canonical·사이트맵 그대로, 랜딩 존 카드와 전 페이지 푸터로 간다. 고아가 되지 않게 도달 가능성 테스트를 박았다. 되돌리려면 탭 한 줄이다. **승격 조건**: 스폰서↔티커 매핑이 열려 바이오 이벤트가 `/api/feed`와 `/stock/{코드}`에 합류하는 시점 — 그때는 서류 목록이 아니라 D-day가 붙은 신호가 된다 |
| 2026-08-23 | **당일 종가·시장 환율 경로 조사**(등록부 §6.5) — KRX 오픈API는 약관상 확정 불가(제6조② 비상업·제11조② 제3자 제공 금지, 유료 옵션 없음), Twelve Data는 공개 표시가 Venture $499/월부터에 외부 배포는 별도 계약, EODHD는 약관이 'displaying'을 금지해 커머셜 필수. 코스콤 종가 FTP가 범위상 최적이나 요금 비공개 → 3곳 문의 초안. 함께 고친 것: **FX 출처 표기 오류**(연준 → 한국은행 ECOS, 상수 하드코딩이 lane 이전을 못 따라간 것)와 매매기준율의 정의상 하루 지연 명시, 공식 종가가 마지막 거래일보다 뒤처질 때 공개 예정 시각 고지 |
| 2026-08-23 | **사전 2차 — 35개 용어, 지표 제목에서 바로 열림** — 화면에 쓰이는데 사전에 없던 17개를 채웠다(장단기 금리차·역레포·TGA·지급준비금·SOFR·연준 총자산·달러지수·하이일드 스프레드·침체 확률·과열도·청산·스테이블코인 공급·가스비·베타·언더워터 곡선·대량보유 5%·주요사항보고). 묶음 2개 신설(매크로와 유동성·분석 지표). `METRICS`에 `term:` 한 필드를 달면 그 지표 카드 **제목이 곧 설명으로 가는 문**이 된다(16개 지표 연결). **함께 고침**: HIP-3 이력 lane이 새 마켓을 최대 6시간 빈 채로 두던 것 — 블롭이 신선해도 시리즈가 아예 없는 심볼만 따로 채운다(실측: `xyz:SKHX` 추가 후 하이닉스 추이선이 몇 시간 비었다). 계속 실패하는 심볼이 매 틱 상류를 두드리지 않도록 시도 시각을 메모리에 기억하고 같은 주기 안에서는 재시도하지 않는다 |
| 2026-08-23 | **용어 사전 `/glossary`** — 화면의 용어 팝오버와 **같은 파일**(`static/terms.json`)을 읽어 18개 용어를 6묶음으로 편다. 사전이 두 벌이면 같은 말이 화면마다 달라지므로 원본은 하나다. **서버 렌더** — 이 페이지의 존재 이유가 "펀딩비 뜻" 같은 검색 유입인데 JS로 채우면 크롤러가 못 읽는다. schema.org `DefinedTermSet` 18항목, `/glossary#{용어}` 딥링크, 팝오버에서 '용어 사전에서 보기 →', 전 페이지 푸터 링크, 사이트맵 등재. 회귀 테스트 6개 — 특히 화면이 부르는 `data-term`이 사전에 다 있는지(오타 나면 클릭해도 조용히 아무 일이 없다) |
| 2026-08-23 | **`/analytics` 콘솔 확산** — 자체 CSS·JS·i18n을 가진 독립 페이지라 셸과 타이포만 옮겼다. **내비 버그**: 이 페이지에서만 다른 화면으로 가는 길이 '시장 모니터'·'종목 분석' 둘뿐이라 /kr·/us·/crypto·/bio에 갈 수 없었다 — 6칸으로 맞췄다. **함정**: 인라인 `<style>`이 `<link>` 뒤에 있어 tokens.css가 같은 특이도에서 지고 있었다(링크를 스타일 블록 뒤로). 가격 lane이 꺼져 있을 때 도는 `applyInsiderMode`가 `.section-heading`·`.section-index`·`.intro-copy`를 참조해 셸 교체로 깨졌던 것도 함께 고쳤다. 한국어인데 탭 제목이 'Stock Analytics · Mulmit'이던 것도 정정. 12px 미만 140곳 → 0곳(차트 축 눈금은 예외) |
| 2026-08-23 | **`/crypto` 콘솔 확산** — 순서를 코인 가격 → 김치프리미엄 → 시장 국면 → 공포·탐욕 → 전체 시장 보드 → 뉴스로 다시 세우고, 해석이 필요한 레인(펀딩·OI·청산·변동성·도미넌스·가스)은 모드 연동 서랍으로. 코인 카드는 `.kro-grid`를 쓰고 있어 /kr에서 만든 타일 시스템을 그대로 물려받는다. 계산 방법(`.kro-method`)은 쉬움에서 접는데, **섹션을 하나하나 적어** 접는다 — 같은 클래스가 바이오의 ClinicalTrials.gov·PubMed·Federal Register **출처 표기**에도 쓰이고 그건 약관상 표시 의무라 어떤 모드에서도 숨기면 안 된다. 12px 미만 319곳 → 0곳 |
| 2026-08-23 | **기준선 판정 + 계기판 숫자** — 공식 종가는 T+1로 확정돼서 아침·주말에는 직전 세션보다 하루 늦다. 그때 "종가 대비"는 이미 끝난 정규장 하루를 포함해 버린다(실측: 삼성전자 −0.27% vs −4.75%). 어느 기준을 앞에 둘지 `overnightReference()` 하나가 정하고 홈 보드와 /kr 카드가 그 판정을 함께 쓴다 — 공식 종가가 늦으면 **"{날짜} 15:30 이후"**, 따라잡으면 "{날짜} 마감 이후". 뒤로 밀린 쪽은 근거 줄에 남는다. 값 변화는 깜빡임 대신 **자리별 0~9 띠가 굴러가는 계기판**(오른쪽부터 18ms씩 시차, 자릿수가 달라지면 굴리지 않음, 띠는 aria-hidden·진짜 값은 별도 노드, `prefers-reduced-motion`이면 즉시 교체). 테이프는 이미 옆으로 흐르므로 짧은 깜빡임 유지 |
| 2026-08-23 | **참고가 라벨 재정의 + 실시간 깜빡임** — `vs_official_percent`는 원래 "마지막 종가 이후 24시간 시장에서 지금까지"의 움직임인데 라벨이 "8/20 종가 대비"라 그날의 일간 등락률처럼 읽혔다. 어느 주식앱에나 있는 값처럼 보이던 것이 사실 이 사이트의 존재 이유였다 — **"{날짜} 마감 이후"** 로 바꿨다(홈·`/kr` 공통, `kro.vsClose`). 값이 바뀌면 짧게 물들이는 깜빡임을 보드 타일과 테이프에 붙였다(직전 값 기억, 첫 렌더는 깜빡이지 않음, `prefers-reduced-motion`이면 없음) |
| 2026-08-23 | **`/kr` 콘솔 확산** — 24시간 참고가 카드를 보드 타일과 같은 시스템으로(렌더러는 그대로, CSS만). 쉬움에서는 '종가 대비' 하나만 남기고 15:30 퍼프 대비·근거표는 전문가에서 펼친다 — 같은 크기 퍼센트 두 개가 나란히 서면 어느 쪽이 오늘 이야기인지 알 수 없다. 경고 칩(지연·저유동성)은 위험 정보라 접지 않는다. 순서는 참고가 → 지수군 → 공시 속보 → 연금·대량보유 → ETF → 주말 신호 → 매크로. 12px 미만 168곳 → 0곳. **버그**: tokens.css의 전역 `.up/.down` 색 규칙이 컨테이너(`.kro-card.up`)에도 걸려 카드 안 글자를 통째로 물들이고 있었다 — 전역 규칙을 걷어내고 값을 그리는 규칙에서만 색을 준다 |
| 2026-08-23 | **`/us` 콘솔 확산** — 22장짜리 '시장 요약' 카드 벽을 걷어내고(딥 섹션이 같은 키를 전부 다시 그리던 순수 중복) 6타일 보드로 대체. 지표 타일은 `window.mulmitMetric`으로 METRICS 정의의 판정을 그대로 받아 쓴다 — 같은 계열이 화면마다 다른 숫자로 나오지 않게. 표·지표 카드의 9~11.5px를 12px 바닥 위로(페이지 내 1,179곳 → 0곳), 지표 섹션은 모드 연동 서랍으로. **정정**: 10년물·SOFR·IORB의 변화를 퍼센트로 쓰던 것을 포인트(%p)로 바꿨다 — 4.65→4.69는 '+0.86%'가 아니라 4bp다 |
| 2026-08-22 | **홈 리디자인(콘솔 셸)** — 히어로 카피를 걷어내고 첫 화면을 값으로 채웠다. 고정 시세 테이프(한 방향 마퀴, 포인터가 닿으면 정지)·세션 인지형 6타일 보드·S&P 500 히트맵·쉬움/전문가 모드·용어 팝오버(`terms.json` 18항목). Pretendard 자체 호스팅(OFL, 92개 구간 분할), 최소 글자 12px, 색은 등락에만. `Intl` compact이 지수를 "7.66천"으로 쓰던 것을 고쳐 전 페이지에 적용. ETH·`xyz:SKHX`를 상시 자산으로 승격(홈 보드 추이선). 판정 갱신은 §2 |
| 2026-08-22 | #16 **청산 집계 레인 구현**(`/api/crypto/liquidations`, `/crypto` 섹션) — Coinalyze 서면 승인(§3.27 `DS-2026-019`) 뒤 키 발급·실측. BTC 24h 롱 $63.6M·숏 $51.8M, ETH $76.9M·$91.6M. **합계는 응답한 거래소들의 합**이며 전 시장 합계가 아니다 — 집계 심볼이 없고 데이터 없는 심볼이 없는 심볼과 똑같이 `200 []`로 와서, 포함·침묵 거래소를 값과 함께 싣는다. BTC 퍼프 28개 중 청산을 주는 곳은 5개. 심볼 1개 = 호출 1회(40/분)라 5분 주기·2코인으로 회당 약 19회. 게이트 `COINALYZE_ENABLED`+`COINALYZE_API_KEY` 기본 OFF |
| 2026-08-22 | #16 **Coinalyze 서면 승인** — 문의 5항(집계 청산 1h/24h 롱·숏, OI, 서버 중계·5분 캐시, 일별 집계 비공개 저장, 광고 포함)을 그대로 허용. 조건은 **dofollow 링크** 하나. 등록부 §3.27 `DS-2026-019`. 하위 거래소 권리는 답하지 않아 미해결로 기록. 코드는 무료 API 키 발급 뒤 실측하고 착수 — 스키마를 추측해 짜지 않는다. 외부 링크 dofollow 회귀 테스트 추가(TradingView §3.2도 같은 조건, 위반 시 건당 $50k) |
| 2026-08-22 | #16 **코인 사이트맵 동적화** — `/sitemap-coins.xml`: 큐레이션 10종 하드코딩 대신 거래소가 지금 상장한 시장 전부. 목록 판정을 `crypto_coin.page_symbols()` 한 곳으로 모아 **페이지 라우트와 사이트맵이 갈라지지 않게** 했다 — 상장폐지·경로 불허 심볼은 라우트가 404를 주므로 사이트맵에도 올리지 않는다. 거래소가 닿지 않으면 큐레이션 코인만(라우트의 폴백과 동일), 섹션 게이트가 꺼지면 빈 사이트맵 |
| 2026-08-22 | #16 Phase 5d — **차트 보조지표**: 코인 페이지 자체 SVG 차트에 이평선(20·50)·볼린저(20±2σ)·RSI(14)·MACD(12·26·9). 계산은 서버(`app/crypto_indicators.py`)에서 하고 시리즈를 캔들과 같은 길이로 실어 보낸다 — 국면 패널이 이미 RSI를 말하고 있어 **같은 이름의 두 숫자가 다른 정의에서 나오면 안 되기** 때문(`crypto_signal.rsi`와 값 일치를 테스트가 검증). RSI·MACD는 아래 페인을 공유하고, 기간이 날짜가 아니라 봉 수라는 근거 문구를 차트 아래에 동봉. 선그리기는 TradingView Advanced Charts 승인 대기 |
| 2026-08-22 | **통합 검색** — 마스트헤드 한 칸에서 코인·국내 종목·미국 종목을 함께 찾는다(`/api/search`, `app/search.py`, `static/search.js`). 세 로스터 모두 **저장된 것만** 읽어 타이핑마다 외부 API를 부르지 않는다(코인=대시보드가 이미 폴링하는 HL 스냅샷 캐시, 국내=금융위 상장 로스터, 미국=EDGAR 수집 대상 티커). 정렬은 완전일치→접두→부분일치이고 코인 동점은 24시간 거래대금으로 가른다. **한 레인이 꺼지거나 실패하면 그 묶음만 빠지고 나머지는 그대로 답한다** — 검색이 한 소스의 가용성에 걸리면 안 되기 때문. 결과는 그대로 `/crypto/{SYMBOL}`·`/stock/{CODE}` 허브로 연결 |
| 2026-08-22 | #16 업비트 김치프리미엄 위험수용 개방·라이브(DS-2026-012), 가스 레인 Alchemy 계정으로 ON(DS-2026-013), Deribit·Coinalyze 문의 발송 |
| 2026-08-22 | #8 식약처 품목허가 lane **라이브** — 운영자가 `MFDS_ENABLED` ON(+`NCBI_EMAIL` 설정), 첫 패스 30일 112품목(허가 72·신고 40·신약 1·희귀 2, 실패 0). 바이오 섹션 5개 레인 전부 라이브 |
| 2026-08-22 | #8 식약처 품목허가 lane(`/api/bio/mfds`, 게이트 OFF) — 운영자 활용신청 승인 후 서버 실측·구현, 등록부 §3.26 `DS-2026-018` |
| 2026-08-22 | #8 바이오 Phase 2 **라이브** — 운영자가 `PUBMED_ENABLED`·`FEDERAL_REGISTER_ENABLED` ON, 첫 패스 PubMed 150건(적중 34, ET 주말 창)·자문위 공고 11건 저장 |
| 2026-08-22 | #8 바이오 Phase 2 — PubMed 서지(임상 표 병합)·FDA 자문위 공고(Federal Register) lane, 게이트 OFF; 식약처 품목허가는 운영자 활용신청 대기(등록부 §3.24~§3.26) |
| 2026-08-22 | #8 바이오 섹션 Phase 1 **라이브** — 운영자가 `BIO_SECTION_ENABLED`·`CLINICALTRIALS_ENABLED`·`OPENFDA_ENABLED` ON, 첫 ingest 패스(임상 34/34·FDA 140건) 저장·`/bio` 확인 |
| 2026-08-22 | #8 바이오 섹션 Phase 1 — `/bio` 페이지·`/api/bio/{trials,fda}`(ClinicalTrials.gov 워치리스트 임상 동향, openFDA 최근 NDA·BLA 승인), 등록부 §3.22·§3.23, 게이트 3종 기본 OFF, 계획 `PLAN_BIO_SECTION.md` |
| 2026-08-22 | #9 NXT 시장정보 조사 종결(`license_required` — 정보포털 계약형, 웹사이트용 고정비·무상 2027-02·유상 1년 약정, 금액 비공개 → 운영자 문의 선택), #11 roic.ai 현시점 기각(재배포 금지·Enterprise 견적만), #2 상태 셀을 P1~P4 완료로 정정 (등록부 §6.3·§6.4) |
| 2026-08-22 | #16 Phase 5c — 크립토 헤드라인(#10 GDELT lane 재활용): 크립토 키워드 6종·코인 이름 사전 10종 추가, `/api/crypto/news`, `/crypto` 뉴스 섹션·코인 페이지 관련 뉴스. 영어 단어와 겹치는 티커(SOL·LINK·SUI·HYPE·ETH·DOGE)는 오태깅 방지로 제외 |
| 2026-08-22 | #16 Phase 5b — 코인 페이지에 원화 시세·김치프리미엄(업비트 KRW 상장 5종: 원화가·USDT 기준·공식환율 기준·테더 프리미엄·원화 거래대금). 대시보드 김프 섹션과 같은 빌더를 재사용해 값이 어긋나지 않게 했고, 게이트가 닫히면 블록이 사라진다 |
| 2026-08-22 | #16 Phase 5a — 과열도·미결제약정 **시계열**(기존 30분 lane에 샘플 적재, 코인·시장 각각 48샘플+90일)과 **포지션 흐름** 해석(가격↔OI 부호 조합: 신규 롱/숏 커버링/신규 숏/롱 정리). 코인 페이지 24h 변화·스파크라인, 카드 배지 ▲▼, 시장 게이지 추이 |
| 2026-08-22 | #16 Phase 4c — 시장 전체 국면(`/api/crypto/regime`: 쏠림 폭·상승 폭·BTC 과열도·공포탐욕 합성)과 대시보드 카드 과열도 배지(ingest 30분 캔들 블롭 + 요청 시점 펀딩 합성). **정정**: HL 펀딩의 고정 이자 성분 때문에 중립 시장도 +10.95% APR이라 과열도가 부풀던 것을 기준선 거리 기준으로 수정 |
| 2026-08-22 | #16 Phase 4b — 코인 페이지 시장 국면 신호(`signal`: 과열도 0~100·추세 −100~+100, 펀딩·변동성 백분위·범위 위치·RSI 4개 구성요소와 가중치 공개, 한 문장 해석). 매수·매도 신호가 아니라 상태 서술이며 "과열=하락 예고 아님" 고지 동봉 |
| 2026-08-22 | #16 Phase 4a — 코인 상세 페이지 `/crypto/{symbol}`(서버 렌더 메타·자체 SVG 캔들 차트·인터벌 4종)와 `/api/crypto/coin/{symbol}`, 대시보드 카드·보드 심볼에서 링크. TradingView 위젯은 표시 거래소 혼동·iframe 추적 때문에 채택하지 않음(§3.2 경로는 열려 있음) |
| 2026-08-22 | #16 정정 — CMC `stablecoin_24h_percentage_change`는 24h **거래대금** 변화(실측 +22.6% vs 시총 $282B): 스테이블 시총 카드의 24h 라벨 제거, 스테이블 거래대금 카드로 이동 |
| 2026-08-22 | #16 Phase 3b — 스테이블코인 공급·유동성(`/api/crypto/structure`의 `stablecoins`: USDT·USDC 유통 공급·페그·비중, 자체 일별 누적 7d/30d), CMC 같은 키(quotes/latest 1크레딧/시간), 등록부 §3.20 사용 범위 갱신 |
| 2026-08-22 | #16 Phase 3a — HL 전체 시장 보드(`/api/crypto/board`: 급등·급락·OI·거래대금 상위·펀딩 극단값·합계), 새 권리 없음 |
| 2026-08-21 | #16 Phase 2b — 가스 스트립 lane(운영자 RPC 계정 주입형, 게이트 OFF), CMC 도미넌스 라이브(DS-2026-011), 총시총 T 포맷, Deribit·Coinalyze 문의 초안 |
| 2026-08-21 | #16 Phase 2 — 도미넌스(CMC, 키 대기)·김치프리미엄(업비트, pending_rights) 레인 구현, 가스 스트립 보류 판정(등록부 §3.19~3.21). Phase 1은 같은 날 머지·배포·게이트 ON으로 라이브 |
| 2026-08-21 | #16 Phase 1 구현 — `/crypto` 페이지·`/api/crypto` 3종(HL 네이티브 퍼프·alternative.me F&G·실현변동성/상관), 등록부 §3.1 단락·§3.18·§4.1 문의 3행, 게이트 2종 기본 OFF |
| 2026-08-21 | #16 크립토 섹션 계획 확정 — 외부 추천안 10항목 검증, 소스 17종 약관 원문·서울 KT망 실측(`PLAN_CRYPTO_SECTION.md`), Deribit·두나무·Coinalyze 문의 초안(`INQUIRY_CRYPTO_SOURCES.md`). Phase 1은 신규 권리 없이 착수 가능, Binance 청산 직결·CoinGecko 무료·CNN F&G 교차는 기각 |
| 2026-08-21 | #17 신설·완료 — VIX·HY 카드 숨김, OFR 금융스트레스 lane, 실현 변동성 카드, 시장 심리 게이지(실험), HIP-3 일봉 이력 개방(DS-2026-001 개정). #16은 크립토 섹션 계획(별도 세션)용으로 예약 |
| 2026-08-20 | #10 뉴스 양축 완료·후보 판정 종결, #12 피드 확장 완료(급변·뉴스·보도자료·5%·지역 필터·15분 주기), 대량보유 전체 보고자 lane 추가 |
| 2026-08-18 | 최초 작성 — 위시리스트 14항목 판정, 실행 순서 확정 |
